import { ThreeDots } from 'react-loader-spinner';

export const Loader = () => {
  return (
    <ThreeDots
      color="#1c2425"
      wrapperStyle={{
        justifyContent: 'center',
      }}
    />
  );
};
